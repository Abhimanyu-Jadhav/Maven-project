pipeline {
    agent any

    triggers {
        githubPush()
    }

    stages {
        stage('Build') {
            when {
                branch 'main'
            }
            steps {
                echo 'Code merged/pushed to main branch'
                // Build commands here
            }
        }
    }
}